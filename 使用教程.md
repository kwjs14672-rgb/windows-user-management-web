# Windows 用户远程管理系统 使用教程

> 版本：2.0.2 ｜ 适用：Windows 7/10/11 及 Windows Server

---

## 一、系统简介

本系统是一个基于浏览器的 Windows 用户远程管理工具，安装在被管理的电脑上，通过网页即可完成：

- 👤 **用户管理**：查看用户列表、添加用户、修改密码、重命名、启用/禁用
- 🔐 **会话管理**：查看谁在登录、强制断开/注销远程用户、给用户发消息
- ⏰ **使用授权**：按时间段控制 Windows 用户的使用权限，到期自动下线
- 🛡️ **安全体检**：检查管理员账户、查看操作日志
- 👨‍💼 **多管理员**：支持多个管理员账号，独立密码

---

## 二、安装

1. 双击 `WindowsUserManager-Setup-2.0.2.exe`，按提示下一步
2. 安装过程中有两个**推荐勾选**的选项：
   - ✅ **放行防火墙端口**（允许其他电脑访问）
   - ✅ **设置开机自启**（开机自动运行，无需登录）
3. 安装完成会自动打开管理页面

**默认登录信息：**
- 访问地址：`http://localhost:8080`（本机）
- 用户名：`administrator`
- 密码：`admin123`

⚠️ **首次登录后请立即修改密码！**

---

## 三、首次登录与改密码

1. 浏览器打开 `http://localhost:8080`
2. 输入 `administrator` / `admin123` 登录
3. 点击左侧/顶部导航 **「系统管理」**
4. 在管理员列表中 `administrator` 那一行点 **「修改密码」**
5. 输入新密码（至少 8 位，建议字母+数字+符号组合）→ 保存

---

## 四、日常使用

### 4.1 用户管理
- 进入 **「用户管理」** 页面
- 可查看所有本地用户、修改密码、添加/删除用户

### 4.2 会话管理
- 进入 **「会话管理」** 页面
- 可看到当前活动的登录会话
- 选中会话可**强制断开**或**注销**该用户

### 4.3 使用授权（限时用户）
- 进入 **「授权管理」** 页面
- 可为指定用户设置**开始/结束日期**
- 到期后系统自动将该用户下线，适合临时/租赁用户管理

### 4.4 安全体检
- 进入 **「安全体检」** 页面
- 查看管理员账户是否安全、历史操作日志

---

## 五、让其他电脑访问（局域网）

1. 安装时勾选了「放行防火墙端口」即可；若没勾选，手动放行：
   - 以管理员身份运行 `C:\Program Files\WindowsUserManager\firewall-open.bat`
2. 查看本机 IP：命令提示符运行 `ipconfig`，记下 IPv4 地址（如 `192.168.1.100`）
3. 其他电脑浏览器访问：`http://192.168.1.100:8080`

---

## 六、修改端口

默认端口 8080。如需修改：

1. 停止服务：
   - 命令行运行 `schtasks /End /TN "WindowsUserManager"`
2. 编辑配置文件 `C:\Program Files\WindowsUserManager\config\admin_config.json`
3. 找到 `settings`，添加或修改：
   ```json
   "settings": {
     "port": 9000
   }
   ```
4. 保存后启动：
   - 命令行运行 `schtasks /Run /TN "WindowsUserManager"`
5. 若启用了防火墙规则，新端口也需要放行（重新运行 firewall-open.bat 或手动添加）

---

## 七、开机自启管理

- **查看自启任务**：`schtasks /Query /TN "WindowsUserManager"`
- **立即启动**：`schtasks /Run /TN "WindowsUserManager"`
- **立即停止**：`schtasks /End /TN "WindowsUserManager"`
- **取消自启**：`schtasks /Delete /TN "WindowsUserManager" /F`

---

## 八、常见问题

### Q1：登录提示"用户名或密码错误"
- 确认用户名是 `administrator`（区分大小写）
- 确认密码正确；忘记密码见 Q2

### Q2：忘记管理员密码怎么办？
1. 停止服务：`schtasks /End /TN "WindowsUserManager"`
2. 编辑 `C:\Program Files\WindowsUserManager\config\admin_config.json`
3. 把 `passwordHash` 的值改为：`698d51a19d8a121ce581499d7b701668`
4. 启动服务：`schtasks /Run /TN "WindowsUserManager"`
5. 用密码 `admin123` 登录，然后立即修改

### Q3：网页打不开 / 连接被拒绝
- 确认服务在运行：`netstat -ano | findstr :8080`（应有 LISTENING）
- 确认端口正确：`Get-Content "C:\Program Files\WindowsUserManager\config\admin_config.json"` 查看 settings.port
- 服务没运行就执行：`schtasks /Run /TN "WindowsUserManager"`

### Q4：程序启动后闪退
- 查看日志：`Get-Content "C:\Program Files\WindowsUserManager\logs\app.log" -Tail 50 -Encoding UTF8`
- 常见原因：端口被占用 → 修改配置文件里的端口

### Q5：其他电脑访问不了
- 确认防火墙已放行（运行 firewall-open.bat）
- 确认访问的是 `http://本机IP:8080` 而不是 localhost
- 确认两台电脑在同一局域网

---

## 九、安全建议

1. **务必修改默认密码**（admin123 是公开默认值）
2. 管理员密码建议 10 位以上、大小写+数字+符号
3. 公网环境访问时，建议搭配防火墙白名单/VPN 使用
4. 定期查看「安全体检」中的操作日志

---

*本系统仅供合法的服务器/电脑管理场景使用。*
