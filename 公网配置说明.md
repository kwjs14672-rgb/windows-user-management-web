# Windows 用户远程管理系统 — 公网访问配置说明

> 版本：2.1.0 ｜ 目的：让任意电脑（互联网上任何地方）通过浏览器访问管理界面
> 原理：frp 内网穿透 —— 通过一台有公网 IP 的服务器中转，把本机 8080 端口映射到公网

---

## 一、架构图

```
任意电脑/手机
    │ 浏览器访问 http://公网IP:8080
    ▼
┌─────────────────────┐
│  公网服务器（frps）  │   ← 如 RackNerd VPS，有公网 IP
│  监听 7000 + 8080   │
└─────────┬───────────┘
          │ frp 隧道（加密）
          ▼
┌─────────────────────┐
│  目标电脑（frpc）    │   ← 装有 WindowsUserManager 的机器
│  本机 8080 端口      │
└─────────────────────┘
```

---

## 二、需要准备什么

| 项目 | 说明 |
|---|---|
| 一台有**公网 IP** 的服务器 | 云服务器（RackNerd/阿里云/腾讯云等）或机房服务器 |
| 服务器系统 | Linux（Ubuntu/Debian/CentOS）或 Windows 均可 |
| 目标电脑 | 装 WindowsUserManager 的机器，能上网即可（无需公网 IP） |

---

## 三、推荐方式：一体化安装包（装完即用）

**安装包**：`WindowsUserManager-Setup-2.1.0.exe`（已内置 frp 客户端）

### 安装步骤
1. 双击安装包，一路下一步
2. 附加任务页（默认全部勾选）：
   - ✅ 放行防火墙端口
   - ✅ 设置开机自启
   - ✅ **启用公网访问**
3. 出现「公网服务器配置」页面时，填写 **frp 公网服务器 IP**（改成你自己的服务器 IP，如 `YOUR_SERVER_IP`）
4. 完成。安装程序自动完成：启动服务 → 配置 frp 隧道 → 设置开机自启 → 打开管理页面

### 完成后
- **本机访问**：`http://localhost:8080`
- **公网访问**：`http://你的公网服务器IP:8080`
- 任意电脑浏览器打开即可，无需安装任何软件

### 修改公网服务器 IP（装错/换服务器时）
编辑 `C:\Program Files\WindowsUserManager\frp\frpc.toml`：
```toml
serverAddr = "新的公网服务器IP"
```
保存后重启 frp：
```powershell
Stop-Process -Name frpc -Force
Start-Process "C:\Program Files\WindowsUserManager\frp\frpc.exe" -ArgumentList "-c","C:\Program Files\WindowsUserManager\frp\frpc.toml" -WindowStyle Hidden
```

---

## 四、手动方式：自己搭 frp 服务端（Linux 服务器）

适合：公网服务器是 Linux，且想完全掌控配置。

### 第1步：在公网服务器上安装 frps（服务端）

```bash
# 下载 frp（v0.61.1，linux amd64）
wget -O frp.tar.gz "https://ghfast.top/https://github.com/fatedier/frp/releases/download/v0.61.1/frp_0.61.1_linux_amd64.tar.gz"
tar -xzf frp.tar.gz
mv frp_0.61.1_linux_amd64 /opt/frp
cd /opt/frp

# 配置服务端（7000 = frp 控制端口）
cat > frps.toml <<'EOF'
bindPort = 7000
auth.method = "token"
auth.token = "改成你自己的随机token"
EOF
```

### 第2步：注册为系统服务（开机自启）

```bash
cat > /etc/systemd/system/frps.service <<'EOF'
[Unit]
Description=FRP Server
After=network.target

[Service]
Type=simple
ExecStart=/opt/frp/frps -c /opt/frp/frps.toml
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable frps
systemctl start frps
systemctl status frps   # 应显示 active (running)
```

### 第3步：放行服务器防火墙端口

```bash
# Ubuntu (ufw)
ufw allow 7000/tcp
ufw allow 8080/tcp

# CentOS (firewalld)
firewall-cmd --permanent --add-port=7000/tcp
firewall-cmd --permanent --add-port=8080/tcp
firewall-cmd --reload
```

> ⚠️ 如果是云服务器（阿里云/腾讯云/RackNerd），还要在**云控制台的安全组**里放行 7000 和 8080 端口！

### 第4步：在目标电脑配置 frpc（客户端）

安装包已内置 frpc，配置文件位置：
`C:\Program Files\WindowsUserManager\frp\frpc.toml`

```toml
serverAddr = "你的公网服务器IP"       # 改成你自己的
serverPort = 7000
auth.token = "和frps.toml里一样的token"

[[proxies]]
name = "wum-web"
type = "tcp"
localIP = "127.0.0.1"
localPort = 8080
remotePort = 8080
```

启动：
```powershell
Start-Process "C:\Program Files\WindowsUserManager\frp\frpc.exe" -ArgumentList "-c","C:\Program Files\WindowsUserManager\frp\frpc.toml" -WindowStyle Hidden
```

---

## 五、验证是否配置成功

### 方法1：本机验证（目标电脑）
```powershell
netstat -ano | findstr :8080
```
看到 `0.0.0.0:8080 LISTENING` = 服务正常。

### 方法2：验证隧道连接（目标电脑）
```powershell
netstat -ano | findstr 7000
```
看到到 `公网IP:7000 ESTABLISHED` = 隧道已建立。

### 方法3：公网访问验证（最重要）
**用手机流量**（关 Wi-Fi）打开：
```
http://你的公网服务器IP:8080
```
能出登录页 = 完全成功 ✅

---

## 六、常见问题排查

| 问题 | 原因 | 解决 |
|---|---|---|
| 手机打不开公网地址 | 隧道没建立 | 目标电脑上 `netstat -ano \| findstr 7000` 查 ESTABLISHED |
| 隧道连不上 | frps 没运行/端口没放行 | 服务器上 `systemctl status frps`；检查安全组放行 7000 |
| 公网能开但登录报错 | 密码不对 | 用 administrator/admin123，登录后立即改 |
| 换端口了公网失效 | frpc 的 localPort 没同步 | frpc.toml 的 localPort 改成新端口并重启 frpc |
| frp 进程没开机自启 | 任务没注册 | 重新运行安装包勾选"启用公网访问"，或手动注册计划任务 |

### frp 端口对应关系速查
- **7000**：frp 控制端口（服务器 ↔ 客户端通信）
- **8080**：Web 服务端口（公网访问端口 = 本机监听端口）

---

## 七、安全建议

1. **改默认密码**：登录后立即把 admin123 改掉（公网暴露任何人可见登录页）
2. **改 token**：frp 的 token 不要用默认值，改成一串随机字符（服务器 frps.toml 和客户端 frpc.toml 要一致）
3. **frp 端口可改**：7000/8080 都可以换成不常用的端口（两端同步改），减少被扫描
4. **定期查看**：管理界面的「安全体检」里看操作日志

---

*文档更新时间：2026-08-13*
